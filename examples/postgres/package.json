{
  "name": "@usai-examples/postgres",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Usai application with a PostgreSQL resource: contract-aware CRUD over a runtime-owned pool.",
  "scripts": { "typecheck": "tsc -p tsconfig.json --noEmit" },
  "dependencies": { "@sakaladev/usai": "workspace:*", "zod": "^4.0.0" },
  "devDependencies": { "typescript": "^5.9.0" }
}
