[@sakaladev/usai](../../README.md) / [index](../README.md) / EnvKind

# Type Alias: EnvKind

```ts
type EnvKind = "string" | "url" | "secret" | "int" | "bool" | "enum";
```

The kinds an environment field can have; `secret` is never printed by `inspect`.
