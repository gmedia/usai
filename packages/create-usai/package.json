{
  "name": "create-usai",
  "version": "0.0.1",
  "description": "Scaffold a new Usai application.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": { "create-usai": "./dist/cli.js" },
  "files": ["dist", "templates"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test src/**/*.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "@types/node": "^24.0.0"
  }
}
