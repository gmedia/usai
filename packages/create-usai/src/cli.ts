#!/usr/bin/env node
// Scaffolder. Implemented with milestone D3 once `usai dev` exists; a
// scaffold that cannot be run is worse than none.
console.error("create-usai is not available yet; see docs/STATUS.md");
process.exit(1);
