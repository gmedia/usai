{
  "extends": "./tsconfig.json",
  // `exclude` is inherited, and the application's project excludes
  // `**/*.test.ts` — which would leave this project with no inputs at all.
  // This is the test files' project; it excludes nothing.
  "exclude": [],
  "include": ["test", "src/**/*.test.ts"]
}
